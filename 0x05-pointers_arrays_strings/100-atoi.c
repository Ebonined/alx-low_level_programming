#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * _atoi - str to int function
 * @s: str variable
 *
 * Return: always int
 */

int _atoi(char *s)
{
        int conv;
   

        conv = atoi(s);

        return (conv);
}
