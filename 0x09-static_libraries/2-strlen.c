#include <string.h>
#include <stdio.h>

/**
 * _strlen - main function
 * @s: string variable
 *
 * Return: Always (0) Success
 */

int _strlen(char *s)
{
	char *tmp;

	tmp = s;

	return (strlen(tmp));
}
